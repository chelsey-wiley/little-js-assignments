//Here is a JS file that will read itself using node

testIt = "Wow I can read this on the screen";

var newArray = testIt.split(" ");

console.log (newArray[3])

var fs = require('fs')
var thisTest = fs.readFileSync(create-to-read.js, utf-8);


get file
get list by splitting into arrray by /n
loop through list
